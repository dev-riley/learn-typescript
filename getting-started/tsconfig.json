{
  // 컴파일할 때 추가할만한 부가적인 옵션들 정의
  // 프로젝트를 타입스크립트로 변환할 때, 타입스크립트가 이 프로젝트를 어떻게 이해할지에 대해서 정의 
  "compilerOptions": {
    "allowJs": true, // => 이 프로젝트 안에 자바스크립트를 허용하겠다.
    "checkJs": true, // => @ts-check(타입 검사기능)와 같은 역할
    "noImplicitAny": true // => 모든 타입에 대해서 최소한 가장 기본적인 any라도 넣어야한다는 뜻.
  }
}

// 타입스크립트 공식 사이트에서 Tools => TSConfig Reference를 통해 다른 옵션들도 알 수 있다.